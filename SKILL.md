---
name: one-source-three-formats
slug: one-source-three-formats
display_name: 一稿三改
display_name_en: One Source Three Formats
displayName: 一稿三改
description: "把一份原创素材改编成长文、60 秒口播稿与 9 图卡片稿，并附事实一致性核对表，不新增未证实信息。当用户说「一稿多投」「这份改成三种」「长文改口播」时使用。 也适用于「一稿三改」「内容复用」「repurpose content」这类说法。"
description_zh: "一份素材产出三种内容形态，并做事实一致性核对"
description_en: "Repurpose one source into three formats with a fact-consistency check"
summary: "一份素材产出三种内容形态，并做事实一致性核对"
category: content-creation
version: 0.1.0
author: 刘玉明
tags: [一稿多投, 一稿三改, 长文改口播, 内容复用, repurpose content]
trigger:
  - 一稿多投
  - 一稿三改
  - 长文改口播
  - 内容复用
  - repurpose content
agent_created: true
---

# 一稿三改 (one-source-three-formats)

把一份原创素材同时改成「长文 + 60 秒口播 + 9 图卡片」三种形态，并保证三稿的事实完全一致。普通改写最容易出的问题是同一数字在三处说法漂移（长文「30%」、口播「三成多」、卡片「约三成」），本技能用「事实表 + 一致性核对」把漂移锁死。

## 何时使用

- 用户说「一稿多投」「一稿三改」「这份改成三种」「长文改口播」「做成图文」「内容复用」「repurpose content」时
- 用户有一份材料要分发到公众号、短视频、朋友圈/小红书等多渠道时
- 用户担心多平台内容数字、时间对不上时

## 运行前提

- 依赖：无第三方依赖，纯文本处理
- 不读取任何本地文件，除非用户明确给出路径
- 不联网、不上传素材；不调用图像、音频生成能力

## 输入契约

| 项 | 必需 | 说明 |
|---|---|---|
| 原始素材 | 必需 | 用户自己的原创内容：文章、笔记、访谈记录、产品说明 |
| 目标受众 | 强烈建议 | 决定措辞与深度；缺失时按素材内容推断并标注 |
| 三种形态渠道 | 可选 | 默认「长文=公众号 / 口播=60 秒短视频 / 图文=9 图卡片」 |
| 语气 | 可选 | 专业 / 亲和 / 犀利；默认亲和，三稿可不同但事实必须同源 |

**缺失处理：**

- 缺受众 → 按素材内容推断目标读者并写明「受众未指定，按内容推断为 XXX」。
- 缺渠道 → 用默认三渠道，并在开头标注。
- 素材是他人作品（如别人公众号全文）→ 先提示版权风险，建议只改写自己拥有权利的内容；用户坚持则用，但文末标注「素材版权归原作者，本改写仅供个人参考」。
- 素材只有观点没有可核查事实 → 跳过事实表与一致性核对，注明「无可核查事实」。

## 执行步骤

1. **素材拆解** — 拆成三块：核心观点、支撑事实（数字、案例、引述）、可删可留的背景。这一步只整理不创作，不新增素材外的事实。

2. **建立事实表（核心硬设计）** — 把素材里所有可核查断言编号：F1 数字、F2 时间、F3 因果关系、F4 引述等。每条记「原文表述 + 类型」。这是后续一致性的唯一基准，三稿共用同一套编号。

3. **产出长文** — 完整论证结构：标题 + 2~4 个小标题 + 正文。段落控制在一屏可读（每段 3~6 句）。写作规格见 `references/writing-specs.md`。

4. **产出口播稿** — 按 60 秒约 240 字写，标注停顿（/）与重音（**加粗**），开头 3 秒必须有钩子（反差/疑问/利益点）。事实项沿用 F 编号。

5. **产出图文卡片稿** — 9 张内：标题卡 1 张 + 内容卡若干（每张一句话） + 结尾引导卡 1 张。每张卡对应事实编号或核心观点。

6. **事实一致性核对（核心价值）** — 逐条检查三稿里的 F 编号项，输出一致性表：编号 / 原文表述 / 长文 / 口播 / 卡片 / 是否一致。发现漂移（如长文「增长 30%」口播「增长三成多」）必须标红并给统一表述。模板见 `templates/three-format-output.md`。

7. **硬性约束**：不新增素材没有的事实；不把推测写成结论；不为迎合节奏夸大数字；三稿语气可不同但事实必须同源，且每个数字/时间/因果在表中只有一种写法。

## 输出格式

Markdown，顺序：事实表 → 长文 → 口播稿 → 图文卡片稿 → 一致性核对表。套用 `templates/three-format-output.md`。一致性表出现红色项时，先给「统一表述建议」再交付。

## 能力边界

- 不生成配图、不配音、不排版成图（只给卡片文案，视觉由用户或设计工具完成）
- 不自动发布到任何平台；不替代排期与分发决策
- 不做未经授权的他人内容改写；素材为他人作品时仅提示风险，不主动越权
- 不做事实核查之外的「真假判断」——素材里写的数字我们默认采信，只负责三稿一致，不验证素材本身真伪
- 不承诺流量、转化等效果

## 常见坑

1. **三稿数字漂移** — 现象：长文写「30%」、口播写「三成多」、卡片写「约三成」，读者跨平台对不上。原因：每稿单独写、没共用基准。规避：步骤 2 先建事实表，三稿只引用 F 编号，最后一致性表强制逐条比对。
2. **口播开头没有钩子** — 现象：口播稿第一句是「今天和大家聊聊 XX」，3 秒没留人。原因：按长文逻辑顺写。规避：按 `references/writing-specs.md` 的节奏标注，前 3 秒必须放钩子（反差/疑问/利益）。
3. **把推测写成结论** — 现象：素材说「用户反馈不错」，三稿写成「用户满意度提升 20%」。原因：为凑事实表硬造数字。规避：推测类内容不进事实表，只在长文里保留「用户反馈不错」原表述。
4. **卡片超 9 张或一张塞太多** — 现象：图文做了 15 张，或单卡写三段话。原因：把长文直接切。规避：按 9 张内分配，每张一句话，标题卡+内容卡+结尾卡结构。
5. **误改他人素材** — 现象：用户丢来别人公众号全文要求「改成三种」。原因：没问版权。规避：缺失处理里的版权提示前置，坚持则标注来源与仅供个人参考。
6. **一致性表只核数字漏了因果与引述** — 现象：数字都对，但长文说「因为 A 所以 B」、口播说「B 导致 A」，因果反了。原因：事实表只收数字。规避：事实表覆盖数字/时间/因果/引述四类，逐类比对。

## 验收用例

- 正常：一篇 1200 字产品说明 → 三稿齐全 + 一致性表无红色项。
- 边界 1：素材含 5 处数字 → 事实表列 5 项，一致性表逐条列出且全一致。
- 边界 2：素材只有观点没有事实 → 注明「无可核查事实」，跳过事实表与一致性核对，仍产出三稿。
- 失败：素材是他人公众号文章全文 → 提示版权风险，建议改用自己的原创素材；用户坚持则标注来源。

## 目录说明

- `references/` — 三种形态的写作规格与事实漂移常见类型
- `templates/` — 三稿输出模板与一致性核对表模板
